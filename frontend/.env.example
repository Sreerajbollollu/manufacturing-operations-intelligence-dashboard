# Leave blank on Vercel to use same-origin /api routes.
VITE_API_BASE_URL=

# Set in Vercel Project Settings, not in committed .env files.
DATABASE_URL=postgresql://USER:ENCODED_PASSWORD@HOST:PORT/DATABASE
